# --- Pinned versions for STABLE Streamlit Cloud deployment ---
# These versions are known to have pre-compiled wheels, avoiding source builds.

# Core AI & LangChain
langchain==0.1.20
langchain-core==0.1.52
langchain-groq==0.1.3

# Data & SQL
# CRITICAL FIX: Pinning numpy and pandas to versions with reliable pre-built wheels.
numpy==1.26.4
pandas==2.2.1
duckdb==0.10.1

# Streamlit & UI
streamlit==1.34.1
plotly==5.20.0

# Data Profiling
# NOTE: Uses the modern ydata-profiling, no obsolete helpers.
ydata-profiling==4.7.0

# Sentence Transformers & CPU-only PyTorch
# This is a robust fix that pins the exact CPU versions of torch.
torch==2.3.0
torchvision==0.18.0
sentence-transformers==2.7.0
rapidfuzz==3.8.1
